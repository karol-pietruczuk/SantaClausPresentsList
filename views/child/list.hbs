<h2>Lista dzieci</h2>
<ul>
    {{#each childrenList}}
            <li>
                <label>
                    {{this.name}}
                    <select>
                        <option value="">brak</option>
                        {{#each ../giftsList}}
                            <option value="{{this.name}}">{{this.name}}</option>
                        {{/each}}
                    </select>
                </label>
                <button>Zapisz</button>
            </li>
    {{/each}}
</ul>
<h2>Dodaj dziecko do listy</h2>
<form action="">
    <label>
        Imię
        <input name="name" type="text" placeholder="wpisz imię..." required>
    </label>
    <button type="submit">Dodaj</button>
</form>